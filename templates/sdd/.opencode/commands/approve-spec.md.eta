---
description: Approve a spec_ready feature; marks it `approved` so /next-feature can implement it.
---

A human is approving the spec for `$ARGUMENTS`. Confirm that `specs/$ARGUMENTS/` contains `requirements.md`, `design.md`, and `tasks.md`, then set the feature's state to `approved` in `feature_list.json`.

Do not start implementation here — `/next-feature` picks up `approved` features and implements them without further questions or approvals.

If the spec is incomplete or ambiguous, say exactly what is missing instead of approving.
