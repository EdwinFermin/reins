[project]
name = "demo-api"
version = "0.1.0"
dependencies = ["fastapi>=0.110", "uvicorn>=0.30"]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.6", "mypy>=1.11"]

[tool.uv]
package = true
